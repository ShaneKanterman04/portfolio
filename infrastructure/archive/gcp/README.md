# Retired GCP deployment

This Terraform configuration describes the portfolio's former two-origin Google
Cloud deployment. It is retained only as migration history and is not used by
the current deployment workflow.

The disabled legacy origin remains available during the Kantercloud soak period
as a manual rollback target. Do not apply this configuration as the production
source of truth.
